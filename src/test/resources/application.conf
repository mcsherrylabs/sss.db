testDb {

  datasource {
    driver = "org.hsqldb.jdbc.JDBCDriver"
    connection = "jdbc:hsqldb:mem:test"
    user = "SA"
    pass = ""
    maxPoolSize = 10
    connectionProperties = "" #"";hsqldb.lob_compressed=true"
    cachePrepStmts = true
    prepStmtCacheSize = 250
    prepStmtCacheSqlLimit = 2048
    useServerPrepStmts = true
    transactionIsolationLevel = "TRANSACTION_READ_COMMITTED"
  }

  viewCachesSize = 100
  useShutdownHook = false
  freeBlobsEarly = false


  //tinyint_col TINYINT NOT NULL,
  //tinyint_col_opt TINYINT,
  //smallint_col SMALLINT NOT NULL,
  //smallint_col_opt SMALLINT,

  ##deleteSql = ["DROP VIEW testview", "DROP TABLE test", "DROP table testVersion", "DROP TABLE testBinary" ]
  createSql = ["CREATE TABLE IF NOT EXISTS test (id BIGINT GENERATED BY DEFAULT AS IDENTITY (START WITH 1, INCREMENT BY 1), strId VARCHAR(256), createTime BIGINT, intVal INTEGER)",
    "CREATE TABLE IF NOT EXISTS testBool (id BIGINT GENERATED BY DEFAULT AS IDENTITY (START WITH 1, INCREMENT BY 1), boolVal BOOLEAN)",
    "CREATE VIEW IF NOT EXISTS  testview AS SELECT strId, intVal FROM test WHERE intVal > 50",
    "CREATE TABLE IF NOT EXISTS testVersion (id BIGINT GENERATED BY DEFAULT AS IDENTITY (START WITH 1, INCREMENT BY 1), strId VARCHAR(256), version BIGINT)",
    "CREATE TABLE IF NOT EXISTS testBinary (id BIGINT GENERATED BY DEFAULT AS IDENTITY (START WITH 1, INCREMENT BY 1), blobVal BLOB, byteVal BINARY)",
    "CREATE TABLE IF NOT EXISTS testIntId (id INT GENERATED BY DEFAULT AS IDENTITY (START WITH 1, INCREMENT BY 1), strVal VARCHAR(50))",
    "CREATE TABLE IF NOT EXISTS testPaged (id INT GENERATED BY DEFAULT AS IDENTITY (START WITH 1, INCREMENT BY 1), status_col INT, strCol VARCHAR(100),    PRIMARY KEY(id));",
    "CREATE TABLE IF NOT EXISTS testPaged2 (id INT GENERATED BY DEFAULT AS IDENTITY (START WITH 1, INCREMENT BY 1), status_col INT, strCol VARCHAR(100),    PRIMARY KEY(id));",
    "CREATE TABLE IF NOT EXISTS testForComp (id INT GENERATED BY DEFAULT AS IDENTITY (START WITH 1, INCREMENT BY 1), status_col INT, strCol VARCHAR(100),    PRIMARY KEY(id));",
    """CREATE TABLE IF NOT EXISTS testRowSerialize (
    id INT GENERATED BY DEFAULT AS IDENTITY (START WITH 1, INCREMENT BY 1),
    char_col CHAR NOT NULL,
    varchar_col VARCHAR(50) NOT NULL,
    longvarchar_col LONGVARCHAR(50) NOT NULL,
    char_col_opt CHAR,
    varchar_col_opt VARCHAR(50),
    longvarchar_col_opt LONGVARCHAR(50),
    bit_col BIT NOT NULL,
    bit_col_opt BIT,
    int_col INT NOT NULL,
    int_col_opt INT,
    bigint_col BIGINT NOT NULL,
    bigint_col_opt BIGINT,
    binary_col BINARY(30) NOT NULL,
    binary_col_opt BINARY(30),
    varbinary_col VARBINARY(50) NOT NULL,
    varbinary_col_opt VARBINARY(50),
    longvarbinary_col LONGVARBINARY NOT NULL,
    longvarbinary_col_opt LONGVARBINARY,
    blob_col BLOB NOT NULL,
    blob_col_opt BLOB,
    PRIMARY KEY(id));""",
    "CREATE TABLE IF NOT EXISTS testNoIdentity (id BIGINT DEFAULT 0, boolVal BOOLEAN)",
  ]

}
